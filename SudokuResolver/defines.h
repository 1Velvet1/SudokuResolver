#include <vector>

using RGBcolour = uint32_t;
using cell = std::vector<uint16_t>;
using board = std::vector<std::vector<uint16_t>>;
using matrice = std::vector<std::vector<uint32_t>>;
using image = std::vector<std::vector<uint32_t>>;
using byte = unsigned char;